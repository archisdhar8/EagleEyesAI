// Backward-compatible import surface. Route components use domain entry files;
// saved specifications and terminal layouts retain their existing JSON shape.
export * from "./shared/workspace-implementations";
